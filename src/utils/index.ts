import {colors} from './colors';
import {fonts} from './fonts';
import {DIMENSIONES} from '../config/constants';
import {AUTH_ROUTES} from '../config/constants';

export {colors, fonts, DIMENSIONES, AUTH_ROUTES}